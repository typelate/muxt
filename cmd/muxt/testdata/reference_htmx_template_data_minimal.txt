muxt generate --use-receiver-type=Server

# The safety property itself: an unframed template calling an HX* helper
# fails muxt check with a field-not-found error naming TemplateData — the
# minimal type no longer carries the htmx surface.
! muxt check
cmpenv stderr want-err.txt

-- want-err.txt --
$WORK/template_routes.go:74:13 ExecuteTemplate "GET /oops Oops(ctx)" *TemplateData[RoutesReceiver, string]
$WORK/template.gohtml:1:38: executing "GET /oops Oops(ctx)" at <.HXRedirect>: field or method HXRedirect not found on TemplateData[RoutesReceiver, string]

TemplateData[RoutesReceiver, string] has:
  Err() error
  Header(string, string) *TemplateData[RoutesReceiver, string]
  MuxtVersion() string
  Ok() bool
  Path() TemplateRoutePaths
  Receiver() RoutesReceiver
  Redirect(string, int) (*TemplateData[RoutesReceiver, string], error)
  RedirectFound(url string) (*TemplateData[RoutesReceiver, string], error)
  RedirectMovedPermanently(url string) (*TemplateData[RoutesReceiver, string], error)
  RedirectMultipleChoices(url string) (*TemplateData[RoutesReceiver, string], error)
  RedirectSeeOther(url string) (*TemplateData[RoutesReceiver, string], error)
  Request() *http.Request
  Result() string
  StatusCode(statusCode int) *TemplateData[RoutesReceiver, string]
  String() string

Error: fail: 1 error
-- template.gohtml --
{{- define "GET /oops Oops(ctx)" -}}{{.HXRedirect "/next"}}done{{- end -}}
{{- define "GET /fine Fine(ctx)" -}}<p>{{- .Result -}}</p>{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"context"
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type Server struct{}

func (Server) Oops(_ context.Context) (string, error) { return "", nil }

// Fine proves the base helpers still work on unframed routes.
func (Server) Fine(_ context.Context) (string, error) { return "ok", nil }
