! muxt generate --use-receiver-type=Server --wire-typelate-sse --output-datastar
cmpenv stderr want-err.txt

-- want-err.txt --
Error: --wire-typelate-sse does not support --output-datastar yet; the datastar patch framing over typelate/sse is planned
-- template.gohtml --
{{define "GET /events sse(Events(ctx, execute))"}}{{.Result}}{{end}}
-- go.mod --
module server

go 1.24
-- server.go --
package server

import (
	"context"
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type Server struct{}

func (Server) Events(_ context.Context, execute func(int) error) error {
	return execute(1)
}
