! muxt generate --use-receiver-type=T
cmpenv stderr want-err.txt

-- want-err.txt --
Error:
  POST /hooks Save(ctx, body)
                        ^^^^
$WORK/template.gohtml:1:35: body parameter must have type io.Reader, got string
$WORK/template.go:16:10: Save is defined here
-- template.gohtml --
{{- define "POST /hooks Save(ctx, body)" -}}<p>{{- .Result -}}</p>{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"context"
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type T struct{}

func (T) Save(_ context.Context, body string) (string, error) {
	return body, nil
}
