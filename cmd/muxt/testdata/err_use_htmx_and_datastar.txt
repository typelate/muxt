! muxt generate --use-htmx --use-datastar --use-receiver-type=Server
cmpenv stderr want-err.txt

-- want-err.txt --
Error: --use-htmx and --use-datastar are mutually exclusive; a project selects one frontend framing flag (mix per route by omitting both and writing the wrappers explicitly)
-- template.gohtml --
{{- define "GET / Home(ctx)" -}}<p>{{- .Result -}}</p>{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"context"
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type Server struct{}

func (Server) Home(_ context.Context) (string, error) { return "hi", nil }
