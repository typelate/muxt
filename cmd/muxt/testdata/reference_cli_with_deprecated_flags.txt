# Test that deprecated flags still work for backward compatibility

# All old flags together
muxt generate --receiver-type=Server --receiver-type-package=example.com --templates-variable=ts --receiver-interface=Interface --routes-func=Routes --template-data-type=Data --template-route-paths-type=Paths --output-file=handler_routes.go

muxt check --templates-variable=ts

exec go test -cover

-- go.mod --
module example.com

go 1.24
-- server.go --
package main

import "context"

type Server struct{}

func (s Server) Index(ctx context.Context) error {
	return nil
}

func (h Server) Admin(ctx context.Context) error {
	return nil
}
-- template.go --
package main

import (
	"embed"
	"html/template"
)

//go:embed *.gohtml
var src embed.FS

var ts = template.Must(template.ParseFS(src, "*"))
-- index.gohtml --
{{define "GET / Index(ctx)"}}
<h1>Index Page</h1>
{{end}}

-- admin.gohtml --
{{define "GET /admin Admin(ctx)"}}
<h1>Admin Page</h1>
{{end}}

-- main.go --
package main

func main() {}
-- template_test.go --
package main

import (
	"net/http"
	"net/http/httptest"
	"testing"
)

func TestDeprecatedFlags(t *testing.T) {
	mux := http.NewServeMux()
	Routes(mux, Server{})

	req := httptest.NewRequest(http.MethodGet, "/", nil)
	rec := httptest.NewRecorder()
	mux.ServeHTTP(rec, req)

	if rec.Code != http.StatusOK {
		t.Errorf("expected status 200, got %d", rec.Code)
	}
}
