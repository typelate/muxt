! muxt generate --use-receiver-type=T
cmpenv stderr want-err.txt

-- want-err.txt --
Error:
  GET /api/ping marshalJSON(Ping(ctx))
                            ^^^^
$WORK/template.gohtml:1:39: marshalJSON requires a result to marshal but Ping(_ context.Context) returns nothing
$WORK/template.go:16:10: Ping is defined here
-- template.gohtml --
{{- define "GET /api/ping marshalJSON(Ping(ctx))" -}}{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"context"
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type T struct{}

func (T) Ping(_ context.Context) {}
