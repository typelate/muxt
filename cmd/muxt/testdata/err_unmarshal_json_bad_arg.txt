! muxt generate --use-receiver-type=T
cmpenv stderr want-err.txt

-- want-err.txt --
  POST /users CreateUser(ctx, unmarshalJSON(form))
                              ^^^^^^^^^^^^^^^^^^^
Error: $WORK/template.gohtml:1:41: the unmarshalJSON wrapper requires exactly one argument, the reserved body identifier: unmarshalJSON(body)
-- template.gohtml --
{{- define "POST /users CreateUser(ctx, unmarshalJSON(form))" -}}<p>{{- .Result -}}</p>{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type T struct{}
