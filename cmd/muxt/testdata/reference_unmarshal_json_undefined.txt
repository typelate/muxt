muxt generate --use-receiver-type=T
muxt check

exec go test -race -count=1

-- template.gohtml --
{{- define "POST /notes CreateNote(ctx, unmarshalJSON(body))" -}}<p>{{- .Result -}}</p>{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

// T does not define CreateNote: muxt synthesizes the method signature, and
// unmarshalJSON(body) synthesizes its parameter as json.RawMessage so
// template-first iteration passes the raw payload through.
type T struct{}
-- template_test.go --
package server

import (
	"context"
	"encoding/json"
	"net/http"
	"net/http/httptest"
	"strings"
	"testing"
)

// impl implements the generated receiver interface by hand; compiling this
// method against TemplateRoutes proves the synthesized parameter type is
// json.RawMessage.
type impl struct {
	got *string
}

func (m impl) CreateNote(_ context.Context, body json.RawMessage) any {
	*m.got = string(body)
	return len(body)
}

func Test(t *testing.T) {
	var got string
	mux := http.NewServeMux()
	TemplateRoutes(mux, impl{got: &got})

	req := httptest.NewRequest(http.MethodPost, "/notes", strings.NewReader(`{"x":1}`))
	rec := httptest.NewRecorder()
	mux.ServeHTTP(rec, req)

	if rec.Code != http.StatusOK {
		t.Errorf("status = %d, want %d", rec.Code, http.StatusOK)
	}
	if want := `{"x":1}`; got != want {
		t.Errorf("method saw body %q, want %q", got, want)
	}
	if want := "<p>7</p>"; rec.Body.String() != want {
		t.Errorf("body = %q, want %q", rec.Body.String(), want)
	}
}
