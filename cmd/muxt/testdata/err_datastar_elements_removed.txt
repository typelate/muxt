! muxt generate --use-datastar --use-receiver-type=Server
cmpenv stderr want-err.txt

-- want-err.txt --
Error: the reserved elements argument was removed; stream events with the wrapper form instead, for example: datastar(sse(Stream(ctx, send)))
-- template.gohtml --
{{- define "GET /events datastar(Stream(ctx, elements))" -}}{{- .Result -}}{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"context"
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type Server struct{}

func (Server) Stream(_ context.Context, send func(string) error) {
	_ = send("hi")
}
