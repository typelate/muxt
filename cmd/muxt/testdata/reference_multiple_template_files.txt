# Tests generation with multiple template source files

muxt generate --use-receiver-type=Server

grep 'indexTemplateRoutes' template_routes.go
grep 'aboutTemplateRoutes' template_routes.go

exists index_template_routes_gen.go
exists about_template_routes_gen.go
cat index_template_routes_gen.go about_template_routes_gen.go template_routes.go

# Verify that per-file functions and interfaces are private (not in public API)
exec go doc -all .
! stdout 'indexTemplateRoutes'
! stdout 'aboutTemplateRoutes'
! stdout 'indexRoutesReceiver'
! stdout 'aboutRoutesReceiver'
stdout 'TemplateRoutes'
stdout 'RoutesReceiver'

exec go test

-- go.mod --
module example.com

go 1.24
-- template.go --
package main

import (
	"embed"
	"html/template"
)

//go:embed *.gohtml
var source embed.FS

var templates = template.Must(template.ParseFS(source, "*.gohtml"))
-- index.gohtml --
{{define "GET / Home(ctx)"}}
<!DOCTYPE html>
<html>
<body>
  <h1>Index</h1>
</body>
</html>
{{end}}
-- about.gohtml --
{{define "GET /about AboutUs(ctx)"}}
<!DOCTYPE html>
<html>
<body>
  <h1>About</h1>
</body>
</html>
{{end}}
-- main.go --
package main

import "context"

func main() {}

type Server struct{}

func (s *Server) Home(ctx context.Context) any {
	return nil
}

func (s *Server) AboutUs(ctx context.Context) any {
	return nil
}
-- main_test.go --
package main

import (
	"net/http"
	"net/http/httptest"
	"testing"
)

func TestRoutes(t *testing.T) {
	mux := http.NewServeMux()
	s := new(Server)
	TemplateRoutes(mux, s)

	tests := []string{"/", "/about"}
	for _, path := range tests {
		rec := httptest.NewRecorder()
		req := httptest.NewRequest("GET", path, nil)
		mux.ServeHTTP(rec, req)

		if rec.Code != http.StatusOK {
			t.Errorf("path %s: expected 200, got %d", path, rec.Code)
		}
	}
}
