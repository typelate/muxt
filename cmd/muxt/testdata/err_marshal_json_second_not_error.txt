! muxt generate --use-receiver-type=T
cmpenv stderr want-err.txt

-- want-err.txt --
Error:
  GET /api/user marshalJSON(GetUser(ctx))
                            ^^^^^^^
$WORK/template.gohtml:1:39: marshalJSON requires the second result of GetUser(_ context.Context) (User, bool) to be an error, got bool
$WORK/template.go:20:10: GetUser is defined here
-- template.gohtml --
{{- define "GET /api/user marshalJSON(GetUser(ctx))" -}}{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"context"
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type User struct {
	Name string `json:"name"`
}

type T struct{}

func (T) GetUser(_ context.Context) (User, bool) { return User{}, true }
