! muxt generate --use-receiver-type=Server
cmpenv stderr want-err.txt

-- want-err.txt --
Error: the reserved sse argument was removed; wrap the call and use the send callback instead, for example: sse(Stream(ctx, send))
-- template.gohtml --
{{- define "GET /events Stream(ctx, sse)" -}}{{- .Result -}}{{- end -}}
-- go.mod --
module server

go 1.24
-- template.go --
package server

import (
	"context"
	"embed"
	"html/template"
)

//go:embed *.gohtml
var templatesFS embed.FS

var templates = template.Must(template.ParseFS(templatesFS, "*"))

type Server struct{}

func (Server) Stream(ctx context.Context, send func(data string) error) {
	_ = send("hi")
}
