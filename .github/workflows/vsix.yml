# Packages the VSCode extension in vscode-plugin/ and uploads the .vsix as a build artifact.

name: VSIX

on:
  pull_request:
    branches: [ "main" ]
    paths: [ "vscode-plugin/**" ]
  push:
    branches: [ "main" ]
    paths: [ "vscode-plugin/**" ]

jobs:

  package:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v6

    - name: Set up Node
      uses: actions/setup-node@v6
      with:
        node-version: "lts/*"

    - name: Package
      working-directory: vscode-plugin
      run: npx --yes @vscode/vsce package

    - name: Upload vsix
      uses: actions/upload-artifact@v7
      with:
        name: muxt-template-syntax-vsix
        path: vscode-plugin/*.vsix
        if-no-files-found: error